package com.horstmann.violet.application.menu.xiaole.SequenceTransfrom;

import java.util.UUID;

public class PackageInfo {
   public String id="EAPK_"+UUID.randomUUID().toString().toUpperCase();//这里取包的信息中的id为唯一值 
}
